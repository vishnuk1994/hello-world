# Basic bash commands for reference

# do-nothing command, simplest command in bash; returns 1  :
:
  # uses
    while {};do :; done
    
# to perform operation :
$(( operation_to_perform ))

# default value for arguments :
defVAL=${1:-"default_value"}
  # sets defVAL to "default_value" if argument 1 is not provied
  
# 
